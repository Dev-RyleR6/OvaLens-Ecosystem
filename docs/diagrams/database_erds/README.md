# Database Entity-Relationship Diagrams (ERDs)

Place PostgreSQL 16 relational database diagrams, table schemas, and foreign key relationships here (`.svg`, `.png`, `.dbml`).

* PostgreSQL 16 Master Central Schema
* SQLite WAL Edge Local Store Schema
